user:
  name: "Adityaomer"
  github: "website_test"

runtime: "python"
  image: "python:3.11-slim"

commands:
  package: "inxi"
  build:
    - "pip install -r requirements.txt"
  start: "python3 bot.py"

repository:
  branch: "main"
  auto_deploy: true

env:
  - ENVIRONMENT:production

user:
  name: string
  github: string

