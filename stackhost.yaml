user:
  name: "Adityaomer"
  github: "website-test"

runtime: 
  runtimes: python
  image: "python:3.11-slim"

commands:
  package:"inxi"
  build:
    - "pip install -r requirements.txt"
  start: "python3 web.py"

repository:
  branch: "main"
  auto_deploy: true

env:
  - ENVIRONMENT:production